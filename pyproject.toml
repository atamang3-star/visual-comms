[project]
name = "vizlib"
version = "0.1.0"
description = "A tiny matplotlib wrapper with a pandas-style plotting API"
requires-python = ">=3.9"
dependencies = ["matplotlib>=3.9", "numpy"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
